/Makefile
/Makefile.old
/blib/
/pm_to_blib
/MYMETA.json
/MYMETA.yml
/XS.c
/XS.bs
*.o
*.obj
*.so
*.bs
/JQ-XS-*.tar.gz

# Where the vendored jq is unpacked and built ("make jq_clean" removes it).
/.jq-build/

# debhelper build artifacts
/debian/.debhelper/
/debian/debhelper-build-stamp
/debian/files
/debian/libjq-xs-perl/
/debian/libjq-xs-perl.debhelper.log
/debian/*.substvars

# rpmbuild artifacts
/rpmbuild/

# Where the release workflow stages packages before uploading them.
/dist/
